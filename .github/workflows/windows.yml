name: Windows Installer

on:
  push:
    tags:
      - v*
  workflow_dispatch:

env:
  QT_MODULES: qtbase qtdeclarative qtmultimedia qtsvg qttools qtshadertools

jobs:
  installer:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set version from tag
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        run: bash scripts/ci/set_version.sh "${GITHUB_REF_NAME}"

      - name: Resolve app version
        id: version
        shell: bash
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            echo "value=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"
          else
            version="$(grep -m1 'project(SonarPractice VERSION' CMakeLists.txt | sed -E 's/.*VERSION ([0-9.]+).*/\1/')"
            echo "value=${version}" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Resolve latest Qt 6 (aqt)
        id: qt
        shell: bash
        run: |
          version="$(bash scripts/ci/resolve_qt_version.sh windows)"
          echo "version=${version}" >> "$GITHUB_OUTPUT"
          echo "Using Qt ${version} (latest 6.x on aqt for windows)"

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ steps.qt.outputs.version }}
          host: windows
          target: desktop
          arch: win64_msvc2022_64
          modules: ${{ env.QT_MODULES }}
          tools: tools_ninja tools_cmake
          cache: true

      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Configure
        shell: bash
        run: |
          cmake -S . -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_PREFIX_PATH="${QT_ROOT_DIR}" \
            -DBUILD_TESTING=OFF

      - name: Build
        shell: bash
        run: cmake --build build --target SonarPractice

      - name: Install Inno Setup
        run: choco install innosetup -y --no-progress

      - name: Create installer
        shell: cmd
        env:
          BUILD_DIR: ${{ github.workspace }}\build
          QTDIR: ${{ env.QT_ROOT_DIR }}
          APP_VERSION: ${{ steps.version.outputs.value }}
        run: build_installer.bat

      - name: Upload installer
        uses: actions/upload-artifact@v4
        with:
          name: SonarPractice-Windows-Installer
          path: Output/SonarPractice_*_Setup.exe
          if-no-files-found: error
